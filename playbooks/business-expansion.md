# Business Expansion

用于 `点位策略`、`商业外扩`、增长与商业化判断。

## 先分题

- 这是 `会员付费型` 还是 `概念版规模+付费型`
- 这是 `规范问题` 还是 `方向问题`

## 默认四层

- 当前成立度
- 商业价值
- 外扩方向
- 扩展风险

## 重点判断

- 能不能同时挂 `scale` 和 `pay`
- 是不是能长成模块或机制，而不是一次性点位
- 有没有打断主场景
- 值不值得沉淀成模板 / 资产 / 规范

## 常见误判

- 只谈转化，不谈规模
- 只谈规模，不谈后续付费承接
- 只看当前页，不看相邻能力和链路
